import type React from "react"
import "@/app/globals.css"
import { Inter } from "next/font/google"
import { ThemeProvider } from "@/components/theme-provider"
import { cn } from "@/lib/utils"

const inter = Inter({ subsets: ["latin"] })

export const metadata = {
  title: "Daniel Campos Soares | Desenvolvedor Full Stack & Entusiasta de Hardware",
  description: "Portfólio pessoal de Daniel Campos Soares, Engenheiro de Computação e Desenvolvedor Full Stack.",
  keywords: "desenvolvedor, full stack, engenheiro de computação, portfolio, Daniel Campos Soares",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="pt-BR" suppressHydrationWarning>
      <body className={cn("min-h-screen bg-background antialiased", inter.className)}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
